--- /Users/lucian/projects/spatialSeed/cult_transcoder/CMakeLists.txt
+++ /Users/lucian/projects/spatialSeed/cult_transcoder/CMakeLists.txt
@@ -107,2 +107,11 @@
 
+# --- libadm (ADM XML generation, Apache-2.0) ------------------------------
+if(NOT TARGET adm)
+    FetchContent_Declare(
+        libadm
+        GIT_REPOSITORY https://github.com/ebu/libadm.git
+        GIT_TAG        v0.12.0 # Or whatever recent tag
+        GIT_SHALLOW    TRUE
+    )
+    FetchContent_MakeAvailable(libadm)
+endif()
+
